<%= simple_form_for(@account) do |f| %>
  <%= f.input :name %>
  <%= f.input :email %>
  <%= f.input :phone %>
  <%= f.input :contact_tokens, "data-pre" => @account.contacts.map(&:attributes).to_json %>
  <br/>
  <%= f.button :submit %>
<% end %>
